<h3>Top styles</h3>
<table class="table">
  <thead>
    <th>Style</th>
    <th>Average</th>
  </thead>
  <% @top_styles.each do |style| %>
    <tr>
      <td><%= style.name %></td>
      <td><%= round(style.average_rating) %></td>
    </tr>
  <% end %>
</table>