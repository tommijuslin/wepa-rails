<%= render @brewery %>

<% if current_user %>
  <%= link_to "Change activity", toggle_activity_brewery_path(@brewery.id), data: {turbo_method: "post"}, class: "btn btn-primary" %>

  <div class="my-3" display="inline-block;">
    <%= edit_and_destroy_buttons(@brewery) %>
  </div>
<% end %>
