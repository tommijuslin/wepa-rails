<h3>Top beers</h3>
<table class="table">
  <thead>
    <th>Beer</th>
    <th>Average</th>
  </thead>
  <% @top_beers.each do |beer| %>
    <tr>
      <td><%= beer.name %></td>
      <td><%= round(beer.average_rating) %></td>
    </tr>
  <% end %>
</table>